# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*
.claude/

# Logs
*.log
npm-debug.log*

# PubLedge serves the handcrafted root site; build.js writes to docs/ for
# validation only (workflow runs build as a sanity check). docs/ is not
# the source of truth and is regenerated each build, so it stays out of git.
docs/

# Internal session/handoff notes — local only, not for the public-facing repo
PubLedge-JIA Session Handoff*.md
.perplexity-research/
_private/

# Environment
.env
.env.local
