# Dependencies
node_modules/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/
*.sublime-*
.claude/

# Logs
*.log
npm-debug.log*

# docs/ is the GitHub Pages publish directory (main branch /docs).
# It is regenerated by `npm run build` but committed because Pages serves it.
# Run `npm run build` before committing to keep it in sync with sources.

# Internal session/handoff notes — local only, not for the public-facing repo
PubLedge-JIA Session Handoff*.md
.perplexity-research/
_private/

# Environment
.env
.env.*

# Eval scratch runs and local agent config — not for the public repo
publedge-eval-*/
.agents/

# --- baseline additions (automated audit 2026-04-21) ---
__pycache__/
*.pyc
dist/
build/
.venv/
venv/

# --- portfolio baseline (2026-07-04): transient dirs ---
handoffs/
working/
