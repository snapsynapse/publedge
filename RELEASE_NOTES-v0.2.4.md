# PubLedge v0.2.4

Corrects mutable publication-status labels that shipped in the v0.2.3 package README and current guides. Package and protocol versions remain explicit, while the canonical publication state records independently verified npm, MCP Registry, Git tag, and GitHub Release observations.

The provider record now preserves the actual partial state: npm, Git tag, and GitHub Release identify v0.2.3, while the MCP Registry remains independently recorded at v0.2.2 pending its own accepted descriptor. The package and protocol contracts remain unchanged.
