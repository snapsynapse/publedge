---
audit_type: accessibility
standard: WCAG 2.1 AA
date: 2026-09-07
auditor: Codex (skill-a11y-audit 3.1.0, axe-core 4.12.1, Puppeteer 25.10.0)
site: http://localhost:8088/ generated from the current source checkout
---

# PubLedge accessibility action adoption

## Dependency and policy

The workflow now pins `snapsynapse/skill-a11y-audit/.github/actions/scan` to commit `6af2c95a56ff058cddf0c6febc71512db1d6d19b`. Independent remote tag inspection resolved annotated tag object `1fccece25b28c99bcbbf26881edc336c586f6d5e` for `v3.1.0` to that commit.

The action selects Node 22 for its own scanner runtime. PubLedge continues to use Node 20 for the site build. The local acceptance run used Node 26.7.0 with the release's pinned axe-core 4.12.1 and Puppeteer 25.10.0 dependencies.

The workflow uses `fail-on: major`. Confirmed critical and serious standards findings block even if they appear in a baseline. Moderate, minor and explicitly best-practice-only findings remain advisory. Unknown-impact violations, potentially major incomplete candidates, route errors, and missing required coverage are inconclusive and cannot pass. PubLedge has no accepted accessibility baseline, and this work did not create or refresh one.

## Route coverage

`npm run build` generated the current `docs/` artifact. The scanner recursively read the generated sitemap index and all seven child sitemaps, rewrote the production origin to the local server, and excluded only the existing non-HTML extensions.

| Child sitemap | Required HTML routes |
|---|---:|
| Records | 10 |
| Authorities | 1 |
| Statutes | 10 |
| Reference | 6 |
| Templates | 6 |
| Bridges | 65 |
| Meta | 62 |
| Total | 160 |

The final scan returned one result for every required route and recorded zero page errors.

## Findings and remediation

The first full major-gate run found zero confirmed violations and 495 serious incomplete candidates, so it correctly exited inconclusive. The candidates reduced to four shared rendering conditions:

- 147 arrow-only back-to-top controls whose visible content axe could not measure as text.
- 72 breadcrumb links whose contrast could not be determined because they relied on color and overlapped adjacent content.
- 56 one-character matrix links that were too short for axe to classify as text.
- 220 timeline text candidates whose backgrounds could not be determined because decorative lines and markers were pseudo-elements.

The source remediation gives the back-to-top control visible text, restores breadcrumb underlines, gives matrix links descriptive visible labels, provides an accessible dark-mode matrix background, and renders timeline decorations as real `aria-hidden` elements. `npm run build` regenerated the managed public tree. No axe rule, required route, finding class, or evidence type was suppressed.

An intermediate run exposed 56 confirmed serious matrix contrast failures after the one-character labels became measurable, while the 220 timeline candidates remained incomplete. The dark-mode matrix background and remaining timeline pseudo-element were then corrected in source.

## Final automated result

| Measure | Result |
|---|---:|
| Required routes scanned | 160 of 160 |
| Route errors | 0 |
| Confirmed critical or serious instances | 0 |
| Moderate, minor or best-practice-only instances | 0 |
| Unknown-impact violation instances | 0 |
| Blocking incomplete instances | 0 |
| Advisory incomplete instances | 0 |
| Gate result | Pass, exit 0 |

The final scan completed at `2026-09-08T01:39:52.472Z`. Its local summary artifact SHA-256 was `20020375d15ee09012ccf917c73a29a4996aa65061a1d61d2c28d924f4ee29e5`.

## Explicit theme verification

The full-sitemap run exercised the system-selected theme. A follow-up source review identified that the matrix background fix also needed the site's explicit `.dark-mode` selector, which can be selected independently of the operating-system preference. The final source mirrors the matrix background correction into that selector and labels each matrix count as its actual measure, `N provision` or `N provisions`.

A real-browser representative check used the site's theme button on `/matrix.html` and `/timeline.html` and observed the class transition from `light-mode` to `dark-mode`. After the 150 ms CSS transition settled, axe-core 4.12.1 reported zero violations and zero incomplete candidates for both routes under each explicit class. This four-case check covers the critical matrix and timeline rendering patterns; it is not a 160-route-by-theme cross-product.

This is automated evidence for the generated sitemap scope. It does not establish full WCAG conformance or replace keyboard, screen-reader, zoom, reflow, forced-colors, or device testing.

## Delivery state

Local source, generated outputs, workflow policy, and this evidence record are prepared for review. Push, hosted Actions execution, public deployment verification, and GitHub issue closure remain separate delivery work.
